# GoogleService-Info.plist contains real Firebase credentials — do NOT commit the real one.
# The placeholder version is tracked; replace it with your own from the Firebase console.
# GoogleService-Info.plist
